import FlowBuilder from '../components/flow/FlowBuilder';

/**
 * Main Index Page - Chatbot Flow Builder
 * 
 * This is a comprehensive chatbot flow builder built with React Flow.
 * Features include:
 * - Drag and drop node creation
 * - Visual flow connections with validation
 * - Node editing through settings panel
 * - Flow validation and save functionality
 * - Extensible architecture for new node types
 */
const Index = () => {
  return <FlowBuilder />;
};

export default Index;
